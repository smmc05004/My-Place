import type { NextPage } from 'next';
import styled from 'styled-components';

const Wrapper = styled.div``;

const Home: NextPage = () => {
	return (
		<Wrapper>
			<p>메인 내용</p>
		</Wrapper>
	);
};

export default Home;
